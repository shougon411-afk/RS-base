# iOSショートカットで登録をワンタップ化する

ブックマークレットの代わりに、iOSの「ショートカット」アプリを使うと、
共有ボタンからの1タップ、あるいは背面タップ・アクションボタンなど
より速い方法で同じ処理を実行できます。

**注意:** この方法は Safari でのみ動作します(Chromeでは同様の自動化フックが
用意されていないため)。`/checkin`のQRスキャンもSafariで問題なく動くので、
受付フロー全体をSafariに統一してください。

## 1. ショートカットを作成する

1. iPhoneの「ショートカット」アプリを開き、右上の「+」で新規作成
2. アクションを2つ追加する:
   1. **「Safariの現在のWebページを取得」**(Get Current Webpage from Safari)
   2. **「Webページ上でJavaScriptを実行」**(Run JavaScript on Webpage)
      - 入力: 直前のアクションの結果(Safariの現在のWebページ)
      - スクリプト欄に `bookmarklet/register.js` の中身(`javascript:`を除いた
        関数部分)をそのまま貼り付ける。中の `API_BASE` と `API_KEY` は
        実際の値に書き換えてください。
3. ショートカットの名前を「問診登録」などにする

## 2. 呼び出し方を選ぶ(お好みで組み合わせ可能)

- **共有ボタンから**: N2017.cgiの画面で共有ボタンをタップ→「問診登録」を選択
  (よく使うショートカットは共有シートの上の方にピン留めできます)
- **ホーム画面にアイコンを追加**: ショートカットの詳細画面→「ホーム画面に追加」
  タップ1回で実行できます(ただし裏でSafariがそのページを開いた状態である必要あり)
- **背面タップ**: 設定→アクセシビリティ→タッチ→背面タップ で「問診登録」を割り当てると、
  iPhoneの背面を2回叩くだけで実行できます
- **アクションボタン**(iPhone 15 Pro以降): 設定→アクションボタンで割り当て可能
- **Siri**: 「Hey Siri、問診登録」でも呼び出せます

背面タップかアクションボタンが一番速く、ブックマーク一覧を開く手間がなくなります。
